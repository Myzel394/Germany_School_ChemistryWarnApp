import { StyleSheet } from "react-native";

export default StyleSheet.create({
    title: {
        fontSize: 36,
        color: "#fff",
        fontFamily: "Montserrat-Black",
        marginBottom: 10,
    },
});
