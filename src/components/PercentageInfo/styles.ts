import { StyleSheet } from "react-native";

export default StyleSheet.create({
    wrapper: {
        alignItems: "center",
        marginVertical: 20,
    },
    info: {
        fontFamily: "Montserrat-Bold",
        fontSize: 22,
        color: "#fff",
        marginBottom: 10,
    },
});
